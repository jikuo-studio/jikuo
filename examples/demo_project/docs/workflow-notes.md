# Workflow Notes

This optional document exists so users can preview a Document Rules change.

Suggested exercise:

1. Run a no-write Document Rules plan that adds this file as context.
2. Review the expected write target and approval phrase.
3. Decide whether to apply the guarded change in a local copy.

The committed demo leaves this file optional so the preview has something real
to add.
