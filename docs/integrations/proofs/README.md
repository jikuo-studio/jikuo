# JIKUO Integration Proof Notes

This directory stores reviewed integration proof notes for client adapters,
including accepted proofs and explicit not-accepted observations.

Accepted strict-mounted lifecycle proofs must link both:

- the pre-turn JIKUO card produced before substantive model work, and
- the later completion-review card for the same governed work.

Narrower proof notes may accept a single adapter surface, such as "Codex GUI
pre-turn additionalContext injection". Those notes must name the narrower
accepted scope and must not be used as full strict-mounted lifecycle proof.

Proof notes must not store raw prompts or raw transcripts. Use compact
turn summaries, runtime card links, semantic-intent status, and pass/fail
observations instead.

Do not treat a checked-in hook file, MCP tool discovery result, or local stdin
smoke as a strict-mounted proof by itself.
